# sootypage game panel (.env)
# Copy to .env and edit.

HOST=0.0.0.0
PORT=2323

# Used to sign login tokens. Change this to a long random string.
JWT_SECRET=change_me_to_a_long_random_secret

# Where Rust servers are installed
RUST_ROOT=/srv/rust

# Initial owner user (created automatically at first boot if DB has no users)
OWNER_USERNAME=dylan
OWNER_PASSWORD=dylan
